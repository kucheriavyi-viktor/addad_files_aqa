{
  "dependencies": {
    "cypress": "^12.17.4",
    "prompt-sync": "^4.2.0"
  },
  "devDependencies": {
    "node-fetch": "^3.3.2"
  }
  // "scripts": {
  //   "test": "echo \"Error: no test specified\" && exit 1",
  //   "cy:open": "cypress open",
  //   "cy:run_chrome": "cypress run --headedless -b chrome"
  // },
}
